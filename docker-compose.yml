services:
  api:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  bot:
    build: .
    command: python bot.py
    env_file:
      - .env
    depends_on:
      - api
      